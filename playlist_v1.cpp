// bei änderungen: git status ->  git commit -am "update [beschreibung]" ->  git push u origin main

#include <iostream>
#include <string>
#include <vector>

struct song {

    std::string artist; 
    std::string title; 
    std::string genre;
    int duration;

};

int main() {
    

    return 0;
}

